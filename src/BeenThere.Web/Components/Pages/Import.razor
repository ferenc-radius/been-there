@page "/import"
@using BeenThere.Core.Interfaces

<PageTitle>BeenThere — Import</PageTitle>

<div class="max-w-2xl mx-auto px-6 py-10 space-y-8">

    <div>
        <h1 class="text-xl font-semibold text-slate-100">Import Routes</h1>
        <p class="text-sm text-slate-400 mt-1">Upload GPX or KML files exported from your tracking app.</p>
    </div>

    @* ── Drop zone ── always first so the CTA is never buried *@
    <label class="relative flex flex-col items-center justify-center gap-4 rounded-2xl border-2 border-dashed border-surface-border bg-surface-raised/50 px-8 py-14 text-center hover:border-brand/60 hover:bg-brand/5 transition-colors cursor-pointer">
        <InputFile OnChange="HandleFilesAsync" multiple accept=".gpx,.kml"
                   class="absolute inset-0 opacity-0 cursor-pointer w-full h-full" />
        <span class="flex size-14 items-center justify-center rounded-full bg-brand/10 text-brand pointer-events-none">
            <Icon Name="upload" Size="28" />
        </span>
        <div class="pointer-events-none">
            <p class="text-sm font-medium text-slate-200">Drop GPX / KML files here</p>
            <p class="text-xs text-slate-500 mt-1">or click to browse · up to 50 files at once</p>
        </div>
    </label>

    @* ── Per-file status list ── *@
    @if (_files.Count > 0)
    {
        <ul class="space-y-2">
            @foreach (var f in _files)
            {
                <li class="flex items-center gap-3 rounded-xl border border-surface-border bg-surface-raised/40 px-4 py-3 text-sm">
                    <span class="flex-1 truncate text-slate-200">@f.Name</span>
                    @switch (f.Status)
                    {
                        case FileStatus.Pending:
                            <span class="text-slate-500 text-xs">Queued</span>
                            break;
                        case FileStatus.Processing:
                            <span class="animate-spin size-4 rounded-full border-2 border-brand border-t-transparent shrink-0" aria-label="Uploading" />
                            break;
                        case FileStatus.Success:
                            <Icon Name="check-circle" Size="16" Class="text-green-400 shrink-0" />
                            break;
                        case FileStatus.Error:
                            <span class="text-red-400 text-xs truncate max-w-[14rem]" title="@f.Error">@f.Error</span>
                            break;
                    }
                </li>
            }
        </ul>
    }

    @* ── Help section ── *@
    <div class="space-y-3">
            <p class="text-xs font-medium uppercase tracking-wide text-slate-500">How to export from your app</p>

            @* Discovery vs recording legend *@
            <div class="flex flex-wrap gap-3 text-xs">
                <span class="inline-flex items-center gap-1.5 rounded-full bg-brand/10 border border-brand/30 px-3 py-1 text-brand">
                    <Icon Name="map" Size="12" />
                    Trail discovery
                </span>
                <span class="inline-flex items-center gap-1.5 rounded-full bg-green-500/10 border border-green-500/30 px-3 py-1 text-green-400">
                    <Icon Name="activity" Size="12" />
                    Records your actual GPS walk
                </span>
            </div>
            <p class="text-xs text-slate-500">BeenThere works best with apps that record your <em>actual</em> GPS track. Trail-discovery apps export the predefined trail geometry — not your real path.</p>

            @* Workflow tip *@
            <div class="rounded-xl border border-brand/20 bg-brand/5 px-4 py-3 text-xs text-slate-300 space-y-1">
                <p class="font-medium text-slate-200">💡 Recommended workflow</p>
                <p>Use <strong>AllTrails</strong> to discover and explore trails. Export the GPX and import it into your navigation app as a guide. Record your actual walk there, then export <em>that</em> recording and import it here.</p>
            </div>

            @* AllTrails *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">AllTrails</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-brand/10 border border-brand/30 px-2 py-0.5 text-[10px] text-brand"><Icon Name="map" Size="10" /> Discovery</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Mobile (iOS / Android)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open the app → <strong>Profile</strong> → <strong>Completed</strong> tab.</li>
                            <li>Tap a recorded activity → <strong>⋮</strong> → <strong>Export</strong> → <strong>GPX</strong>.</li>
                            <li>Share or save to Files / Downloads, then upload above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (alltrails.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Profile → Completed → click a recording → <strong>Download GPX</strong>.</li>
                        </ol>
                    </div>
                    <p class="rounded-lg bg-surface-overlay border border-surface-border px-3 py-2 text-slate-400">
                        <strong class="text-slate-300">AllTrails exports the trail's predefined route, not your GPS recording.</strong>
                        This still works great for logging trails you've completed — just know that others who walked the same trail will import identical geometry.
                        For your personal GPS track, record your walk with Komoot, Strava, or Locus Map alongside AllTrails.
                    </p>
                </div>
            </details>

            @* Komoot *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Komoot</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-brand/10 border border-brand/30 px-2 py-0.5 text-[10px] text-brand"><Icon Name="map" Size="10" /> Discovery</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (komoot.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open a completed tour on <strong>komoot.com</strong>.</li>
                            <li>Click <strong>⋮ More</strong> → <strong>Download GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Mobile (iOS / Android)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open a completed tour → tap <strong>⋮</strong> → <strong>Export GPX</strong>.</li>
                            <li>Share to Files / Downloads, then upload above.</li>
                        </ol>
                    </div>
                    <p class="text-slate-500">Komoot has built-in trail discovery <em>and</em> records your actual GPS track — the best of both worlds. Elevation and heart rate included when available.</p>
                </div>
            </details>

            @* Wikiloc *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Wikiloc</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-brand/10 border border-brand/30 px-2 py-0.5 text-[10px] text-brand"><Icon Name="map" Size="10" /> Discovery</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (wikiloc.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to your <strong>Profile</strong> → <strong>Trails</strong>.</li>
                            <li>Open a trail → click <strong>Download</strong> → select <strong>GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Mobile (iOS / Android)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open the <strong>Wikiloc</strong> app → tap <strong>My Trails</strong>.</li>
                            <li>Tap a trail → <strong>⋮</strong> → <strong>Export GPX</strong>.</li>
                            <li>Share to Files / Downloads, then upload above.</li>
                        </ol>
                    </div>
                    <p class="text-slate-500">Wikiloc is a community trail-sharing platform. When you record your own walk, the exported GPX contains your actual GPS track.</p>
                </div>
            </details>

            @* Locus Map *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Locus Map 4</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">Android</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Single track</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open <strong>My Library</strong> → tap your track.</li>
                            <li>Tap <strong>⋮</strong> → <strong>Export</strong> → select <strong>GPX</strong> → <strong>EXPORT</strong>.</li>
                            <li>Save to device storage, then upload above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Multiple tracks</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>My Library → folder → topbar menu → <strong>Select</strong>.</li>
                            <li>Check tracks → bottom-right tool menu → <strong>Export</strong> → <strong>GPX</strong>.</li>
                        </ol>
                    </div>
                    <p class="text-amber-400/80">⚠ Since September 2025 Locus Map no longer exports directly to Google Drive. Export to device storage and transfer manually.</p>
                    <p class="text-slate-500">Locus Map exports include heart rate, cadence, and power data when your device recorded them.</p>
                </div>
            </details>

            @* Gaia GPS *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Gaia GPS</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Export a recorded track</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open the <strong>Gaia GPS</strong> app → tap <strong>Saved</strong> → select a track.</li>
                            <li>Tap the <strong>share icon</strong> → <strong>Export</strong> → select <strong>GPX</strong>.</li>
                            <li>Save to Files / Downloads, then upload above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (gaiagps.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open a recorded track on <strong>gaiagps.com</strong>.</li>
                            <li>Click <strong>Export</strong> → <strong>GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <p class="text-slate-500">Gaia GPS is the best hiking navigator for iOS — excellent offline topo maps, reliable GPS recording, and clean GPX export including elevation and timestamps.</p>
                </div>
            </details>

            @* Garmin Connect *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Garmin Connect</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (connect.garmin.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Activities</strong> and open an activity.</li>
                            <li>Click the <strong>⚙ gear icon</strong> (top right) → <strong>Export to GPX</strong>.</li>
                            <li>The file downloads to your computer — upload it above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Bulk export</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Account Settings</strong> → <strong>Data Management</strong> → <strong>Export Your Data</strong>.</li>
                            <li>Click <strong>Export Data</strong> — Garmin sends a ZIP to your email.</li>
                            <li>Unzip and upload the <code>.gpx</code> files from the <code>Activities/</code> folder above.</li>
                        </ol>
                    </div>
                </div>
            </details>

            @* Strava *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Strava</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Single activity (Web)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open an activity on <strong>strava.com</strong>.</li>
                            <li>Click <strong>⋮</strong> (bottom right of the map) → <strong>Export GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Bulk export</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Settings</strong> → <strong>My Account</strong> → <strong>Download or Delete Your Account</strong>.</li>
                            <li>Click <strong>Request Your Archive</strong> — Strava emails you a download link.</li>
                            <li>Unzip and upload the <code>.gpx</code> files from the <code>activities/</code> folder above.</li>
                        </ol>
                    </div>
                    <p class="text-amber-400/80">⚠ Strava's bulk export may include <code>.fit.gz</code> or <code>.tcx</code> files in addition to GPX — only upload the <code>.gpx</code> files.</p>
                </div>
            </details>

            @* Polar Flow *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Polar Flow</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (flow.polar.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Training</strong> and click a session.</li>
                            <li>Click the <strong>Export</strong> button (top right) → <strong>Export GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Mobile (iOS / Android)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open the <strong>Polar Flow</strong> app → tap a training session.</li>
                            <li>Tap the <strong>share icon</strong> → <strong>Export GPX</strong>.</li>
                            <li>Save to Files / Downloads, then upload above.</li>
                        </ol>
                    </div>
                </div>
            </details>

            @* Suunto *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Suunto</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (suunto.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Activities</strong> and open an activity.</li>
                            <li>Click <strong>⋮</strong> → <strong>Export</strong> → <strong>GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Mobile (iOS / Android)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Open the <strong>Suunto</strong> app → tap an activity.</li>
                            <li>Tap <strong>⋮</strong> → <strong>Export</strong> → <strong>GPX</strong>.</li>
                            <li>Share to Files / Downloads, then upload above.</li>
                        </ol>
                    </div>
                </div>
            </details>

            @* Runkeeper *@
            <details class="rounded-xl border border-surface-border bg-surface-raised/60 text-sm group">
                <summary class="flex cursor-pointer select-none items-center gap-3 px-4 py-3 list-none">
                    <span class="flex-1 font-medium text-slate-200">Runkeeper</span>
                    <span class="inline-flex items-center gap-1 rounded-full bg-green-500/10 border border-green-500/30 px-2 py-0.5 text-[10px] text-green-400"><Icon Name="activity" Size="10" /> GPS</span>
                    <span class="text-xs text-slate-500">iOS · Android · Web</span>
                    <Icon Name="chevron-right" Size="14" Class="text-slate-500 group-open:rotate-90 transition-transform" />
                </summary>
                <div class="px-4 pb-4 pt-1 space-y-3 text-slate-400 text-xs">
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Web (runkeeper.com)</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Activities</strong> and open an activity.</li>
                            <li>Click <strong>Export Activity</strong> (bottom of the page) → <strong>GPX</strong>.</li>
                            <li>Upload the downloaded file above.</li>
                        </ol>
                    </div>
                    <div>
                        <p class="font-medium text-slate-300 mb-1">Bulk export</p>
                        <ol class="list-decimal list-inside space-y-1">
                            <li>Go to <strong>Settings</strong> → <strong>Export Data</strong>.</li>
                            <li>Choose <strong>GPX</strong> and click <strong>Export</strong> — Runkeeper emails you a ZIP.</li>
                            <li>Unzip and upload the <code>.gpx</code> files above.</li>
                        </ol>
                    </div>
                </div>
            </details>

        </div>

</div>

@code {
    [Inject]
    private IImportService ImportService { get; set; } = default!;

    [Inject]
    private ICurrentUserService CurrentUser { get; set; } = default!;

    private readonly List<FileState> _files = [];
    private async Task HandleFilesAsync(InputFileChangeEventArgs e)
    {
        if (CurrentUser.UserId is not { } userId) return;

        var files = e.GetMultipleFiles(50).ToList();

        // Buffer ALL browser file streams into MemoryStreams eagerly before any StateHasChanged()
        // calls. Blazor's BrowserFileStream is backed by JS interop and can be closed/invalidated
        // after re-renders — buffering upfront avoids "Cannot access a closed Stream" errors.
        var batch = new List<FileState>(files.Count);
        foreach (var file in files)
        {
            var state = new FileState(file.Name);
            batch.Add(state);
            try
            {
                await using var raw = file.OpenReadStream(maxAllowedSize: 50 * 1024 * 1024);
                await raw.CopyToAsync(state.Buffer);
                state.Buffer.Seek(0, SeekOrigin.Begin);
            }
            catch (Exception ex)
            {
                state.Status = FileStatus.Error;
                state.Error = $"Could not read file: {ex.Message}";
            }
        }

        _files.AddRange(batch);
        StateHasChanged();

        foreach (var state in batch.Where(s => s.Status == FileStatus.Pending))
        {
            state.Status = FileStatus.Processing;
            StateHasChanged();

            try
            {
                var result = await ImportService.ImportAsync(state.Buffer, state.Name, userId);
                state.Status = result.IsSuccess ? FileStatus.Success : FileStatus.Error;
                if (!result.IsSuccess)
                {
                    state.Error = result.Error;
                }
            }
            catch (Exception ex)
            {
                state.Status = FileStatus.Error;
                state.Error = ex.Message;
            }

            StateHasChanged();
        }
    }

    private enum FileStatus { Pending, Processing, Success, Error }

    private sealed class FileState(string name)
    {
        public string Name => name;
        public MemoryStream Buffer { get; } = new MemoryStream();
        public FileStatus Status { get; set; } = FileStatus.Pending;
        public string? Error { get; set; }
    }
}
