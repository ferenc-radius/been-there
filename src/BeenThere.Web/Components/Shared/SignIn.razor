@* SignIn component with Google OAuth redirect *@

<a href="/signin" class="btn btn-primary">
    Sign In with Google
</a>

@code {
    // The actual sign-in redirect happens via /signin route in Program.cs
}
